#include "main.h"

/**
 * function that checks for uppercase character
 * return: always (success)
 *
 */
 int _isupper(int c);
	int main (c);{ 
	    c = 'A';
    	    printf("%c: %d\n", c, _isupper(c));
    	    c = 'a';
	    printf("%c: %d\n", c, _isupper(c));
    return (0);
}

